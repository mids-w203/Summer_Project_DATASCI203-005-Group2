# prompt

The assignment prompt. Per the course template this holds `assignment.qmd` and
its rendered `assignment.pdf`.

> These files are not yet in the repo — add the instructor-provided
> `assignment.qmd` / `assignment.pdf` here.
